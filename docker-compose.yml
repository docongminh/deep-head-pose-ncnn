version: "3"
services:
  detect:
    image: cristiando0902/head_pose
    command: bash
    # command: bash /source/init.sh
    volumes: 
      - ./:/source
    container_name: "deep-head-pose"
    tty: true
